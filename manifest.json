{
    "name": "${modName}",
    "entry": "${modFile}",
    "version": "${modVersion}",
    "type": "native",
    "platform": "${modPlatform}",
    "optionalDependencies": [
        {
            "name": "iListenAttentively"
        }
    ]
}
